name: Sync Release to SoftwareCenter

on:
  release:
    types: [published]

jobs:
  sync:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up variables
        id: vars
        run: |
          echo "REPO_NAME=${GITHUB_REPOSITORY#*/}" >> $GITHUB_OUTPUT

      - name: Download release assets
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_TOKEN }}
        run: |
          mkdir -p release_assets
          gh release download "$GITHUB_REF_NAME" \
            --repo "$GITHUB_REPOSITORY" --dir release_assets

      - name: Clone SoftwareCenter
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_TOKEN }}
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "action@github.com"
          git clone https://x-access-token:${GH_TOKEN}@github.com/PlaxtonFlarion/SoftwareCenter.git SoftwareCenter

      - name: Clean target directory
        run: |
          rm -rf SoftwareCenter/${{ steps.vars.outputs.REPO_NAME }}
          mkdir -p SoftwareCenter/${{ steps.vars.outputs.REPO_NAME }}

      - name: Copy files
        run: |
          cp README.md SoftwareCenter/${{ steps.vars.outputs.REPO_NAME }}/ || true
          cp LICENSE.md SoftwareCenter/${{ steps.vars.outputs.REPO_NAME }}/ || true
          cp release_assets/* SoftwareCenter/${{ steps.vars.outputs.REPO_NAME }}/ || true

      - name: Push to SoftwareCenter
        run: |
          cd SoftwareCenter
          git add .
          git commit -m "Sync ${{ steps.vars.outputs.REPO_NAME }} release $GITHUB_REF_NAME"
          git push
